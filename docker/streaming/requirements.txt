# ============================================================
# Dependensi image streaming (producer + consumer)
# ============================================================

# Kafka client (wheel sudah membundel librdkafka)
confluent-kafka==2.5.0

# HTTP client untuk polling GBFS
requests==2.32.3

# Validasi schema payload GBFS
pydantic==2.9.2

# Google Cloud: tulis raw layer
google-cloud-storage==2.18.2
google-cloud-bigquery==3.26.0
db-dtypes==1.3.0
pandas==2.2.3
pyarrow==17.0.0

# Baca .env saat dijalankan lokal (di Docker pakai env_file)
python-dotenv==1.0.1
